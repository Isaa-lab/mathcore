L3b: wrong book & review plan end-to-end closed loop

打通 "错题 → SM2 → 每日计划 → 练习 → 回错题" 闭环。
所有 UI 不再读硬编码，改为跨会话持久化数据。

存储地基
- src/utils/storage.js: mathcore:<name>:v1 命名空间包装，读写失败静默降级
- src/utils/wrongItems.js: 错题表 + 独立 SM2（区别于记忆卡 mc_sr）
  · recordWrongAnswer：首次入库 / 再错时 ease-0.2 + 重置 reps + 1 天到期
  · recordCorrectAnswer：SM2 推进（1→3→interval*ease），连续对 3 次且间隔 ≥ 14 天
    自动 mastered。再错一次从 mastered 复活为 active
  · getDueWrongItems / getChapterMistakeStats / tag / suspend / variants 计数
  · ERROR_TAGS 单源：formula/concept/careless/method

答题入口接入
- App.js recordAnswer 里调 record*，覆盖所有答题场景（题库/冲刺/重做）
  · 失败吞掉，不影响答题体验
  · 携带 chapter 便于错题本聚合

错题本 WrongPage 重构
- 数据源：硬编码 4 道 + sessionAnswers → 持久化 wrong_items
- 薄弱章节：从错题表跨会话聚合（不再只看当前 session）
- 筛选：章节 / 状态（待复习/已掌握/全部）/ 错因
- 批量：多选 → AI 变式出题（沿用 /api/generate 接口）/ 原题重做
- 今日到期 CTA：一键复习到期错题
- WrongItemRow：错因多选 chip、SM2 可视化、问 AI、重做、隐藏菜单
- 空状态 / 孤儿错题降级

复习计划 ExamPlanSection 重构
- 章节池：硬编码 8 项 → getUserChapters(ALL_QUESTIONS) 聚合题库 + 错题本
  · 显示每章错题数徽章，引导用户优先勾选薄弱章节
- 每日计划：硬编码模板 → planGenerator 动态生成
  · phase（light/normal/high/sprint）按距考试天数决定强度
  · 任务类型：sm2_due / chapter_practice / mock_exam / light_review
  · 章节按薄弱度 + 按日轮转（避免天天只做同一章）
  · dailyMinutesTarget 硬上限裁任务（默认 60min，可 20-180 调节）
- 日历格子：CalendarDayCell + ProgressRing，显示完成进度 + phase 徽章
- 重生成时保留 completed 状态（避免用户已完成的任务被刷新清空）
- 删除计划时同时清理 exam_plan 持久化

今日任务 TodayPlanView
- 从 exam_plan.daily_plans[today] 渲染
- 每日挂载顺延昨日未完成的 high / sm2_due 任务（打前缀 [昨日未完成]）
- 其余低优未完成：UI 顶部提示"📌 昨日还有 N 个低优任务未完成"
  不自动顺延，避免滚雪球
- TaskCard：开始（跳题库/对话）/ ✓ 手动完成
- 全部完成后显示庆祝条
