feat(viz): provider-aware VIZ prompts + auto silent retry on parse fail

核心问题：Groq/Gemini 结构化输出能力弱，画 [VIZ:...] 时括号/反斜杠经常失衡，
用户看到大量"这张图没画成功"。根因不是渲染器，是不同模型指令遵循偏好不同，
需要对症下药。

Backend (api/generate.js):
- 新增 resolveLikelyProvider() 推断本次请求最终落在哪个 provider
- 新增 VIZ_PROFILE：每个 provider 的专属 VIZ 能力画像
  · Groq: 只允许 hierarchy/process/comparison 三种最浅结构，禁用
    annotation/parametric，每轮限制 1 个 [VIZ:...]
  · Gemini: 同样禁用 annotation/parametric，提示优先用 LaTeX
  · Kimi: 偏保守的四种结构
  · Claude/DeepSeek: 放开所有结构，最多 2 个
- socratic / tutor / chat 三个 prompt 分支都自动注入该 provider 的专属约束
- 顺带修一处字符串字面量里中英双引号嵌套的语法隐患

Frontend (src/App.js - QuizPage):
- sendChatMessage 回复完成后自动检查 splitQuizChatBlocks 是否命中 parseError；
  有则 180ms 后触发 silentlyRetryFailedViz()
- silentlyRetryFailedViz: 不 push 新用户气泡，history 里塞 assistant(failed) +
  user(重画指令含错误原因)，请 AI 重做该条回复
- 只重试一次（vizRetryExhausted 标记防循环），第二次仍失败才让 FailedVizCard
  浮出来
- 重画中 UI：淡色动画占位卡替换红色错误，文案"AI 正在重画这张图…"
- 文案与诊断分离：重试耗尽后提示"换个 AI 试试 / 改用文字讲解"

MaterialChatPage 的前端静默重试留到下一轮；本轮 backend 的 provider-aware
约束已对它全局生效，预期也会显著减少 VIZ 翻车率。
